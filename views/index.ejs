<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <title>
        Main Page
    </title>
</head>
<body>

    <div class="mainDiv">
        <% if (auth) { %>
            <h1>Welcome,  <%=userLogged%> </h1>
            <table>
                <tr>
                    <th>Username</th>
                    <th>Password</th>
                </tr>
                <% for (i in users) { %>
                    <tr>
                        <td><%=i%></td>
                        <td><%=users[i]%></td>
                    </tr>
                <% } %>
            </table>
        
            <%} else { %>
            <h1>Unauthorized visitor, please <a href="/login">login</a> or <a href="/signup">signup</a></h1>
        <% } %>
    
    </div>

    <%- include('partials/footer.ejs');%>
</body>
</html>